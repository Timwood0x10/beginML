"""
Math Lab — interactive numerical visualizations.

Pure-numpy / scikit-learn compute functions. Each module returns plain
JSON-serializable dicts consumed by the React frontend via the /api/lab router.
"""
